// src/shared/utils/filter-types.enum.ts
export enum FilterTypeEnum {
  EXACT = 'exact',
  PARTIAL = 'partial',
  IN = 'in',
  RANGE = 'range',
  BOOLEAN = 'boolean',
  RELATION = 'relation',
  NEGATION = 'negation',
  ARRAY_EXACT = 'ARRAY_EXACT',
  UUID = 'UUID',
}
